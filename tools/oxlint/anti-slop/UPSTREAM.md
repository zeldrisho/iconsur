# Upstream provenance

- **Source repository:** https://github.com/dmmulroy/anti-slop
- **Source revision:** Unknown; installed from the `install-anti-slop` skill bundle supplied by `vpx skills use dmmulroy/anti-slop -s install-anti-slop`.
- **Installed entry point:** `tools/oxlint/anti-slop/index.ts`
- **Included paths:** `index.ts`, `rules/`, `shared/`, and `effect/`.
- **Intentional deviations:** Generic rules are enabled in `vite.config.ts`; the optional Effect plugin is not enabled because `effect` is not a direct project dependency.
- **License/provenance files:** The supplied bundle contained no upstream `LICENSE` or `UPSTREAM.md` file; revision identity remains unknown rather than guessed.
